{
  "name": "atoma-agents",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "turbo test"
  },
  "devDependencies": {
    "@turbo/gen": "^1.13.4",
    "prettier": "^3.5.2",
    "turbo": "^1.13.4"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18.0.0"
  }
}